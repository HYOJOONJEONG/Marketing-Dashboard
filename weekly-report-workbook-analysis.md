# 新주간실적보고 2026.xlsm 분석 메모

## 1. 파일 개요

- 파일명: `新주간실적보고 2026.xlsm`
- 위치: `\\10.150.0.179\정보사업본부\마케팅폴더(정보사업본부)\1.수시 확인 자료\新주간실적보고 2026.xlsm`
- 매크로 포함 여부: 포함 (`xl/vbaProject.bin` 존재)
- 시트 수: 7

확인된 시트

- `주간실적보고`
- `옵션정보입력`
- `신규계약리스트`
- `백데이터` (숨김)
- `Sheet2`
- `Sheet3` (숨김)
- `Sheet1` (숨김)

## 2. 현재 업무 흐름 이해

이 파일은 단순 보고서가 아니라 아래 3단 구조로 운영되고 있다.

1. `신규계약리스트`
   - 신규 계약 원본/마스터 입력 시트
   - 권유자별, 월별 통계가 같은 시트 우측에서 `COUNTIFS`로 집계됨
   - `Update` 버튼 매크로 존재

2. `옵션정보입력`
   - 파일 내부에 직접 적힌 문구대로 `수동 업데이트 영역`
   - 매출, 순증, 해지, 옵션 사용 건수, 업종별 실적, 추가 매출 리스트를 사람이 매주 갱신하는 입력 허브

3. `주간실적보고`
   - 실제 보고서 출력용 전면 시트
   - 신규 계약 리스트 일부를 버튼으로 불러오고, 나머지 KPI/통계는 사람이 맞춰 넣거나 붙여넣는 구조로 보임
   - 인쇄영역이 지정되어 있으며 PDF 보고서의 원본 역할

보조 시트

- `백데이터`
  - 현재 보고서에 실리는 계약 목록만 간략히 저장하는 중간 버퍼 역할
  - `주간실적보고`의 상단 계약 리스트와 동일한 레이아웃

## 2-1. 사용자 확인 기반 실제 운영 프로세스

사용자가 설명한 실제 주간 업무 순서는 아래와 같다.

1. 영업담당자들이 `신규계약리스트` 탭에 각자 계약 리스트를 업데이트
2. 매주 수요일 담당자 1명이 `실적반영(A열)`에 반영 여부를 선택한 뒤 `Update` 실행
3. `주간실적보고` 탭에서 `초기화` 후 `계약호출`을 실행하면 반영된 실적이 상단 계약 리스트로 올라옴
4. `옵션정보입력` 탭은 수동 업데이트 영역이므로 담당자가 직접 입력하고, 그 값이 최종 보고 수치가 됨
5. 완성된 `주간실적보고`를 PDF로 인쇄하여 보고

즉, 현재 프로세스의 핵심은 아래 두 갈래다.

- 자동 반영 흐름: `신규계약리스트` -> `실적반영 선택` -> `Update` -> `주간실적보고 계약호출`
- 수동 입력 흐름: `옵션정보입력` 직접 수정 -> `주간실적보고` 하단 지표 반영

## 2-2. 웹 전환 시 유지되어야 하는 업무 개념

웹으로 바꿔도 아래 개념은 유지하는 것이 맞다.

### 1. 계약 원장 입력 주체 분리

- 영업담당자가 각자 자기 계약을 입력/수정
- 운영 담당자 1명이 주간 반영 대상을 확정

### 2. 주간 반영 확정 단계 존재

- 모든 계약이 자동으로 보고서에 들어가면 안 되고
- `실적반영`처럼 주간 보고에 포함시킬 대상을 선택하는 단계가 필요

### 3. 수동 KPI 입력 단계 유지

- `옵션정보입력`은 현재도 자동 산출이 아니라 수동 확정값이므로
- 웹에서도 별도 입력 화면으로 남겨야 함

### 4. 최종 산출물은 PDF

- 웹 화면이 최종 목적이 아니라
- 최종 승인/보고용 PDF 출력이 끝단이어야 함

## 3. 확인된 매크로 진입점

버튼에 연결된 매크로 이름은 아래와 같다.

### `주간실적보고`

- `PasteDataToWeeklyReportWithBorders`
  - 버튼명: `계약호출`
  - 위치: `O3`
  - 역할 추정: 선택/복사된 계약 데이터를 보고서 상단 표에 붙여넣고 테두리까지 맞춤

- `Module4.ClearRange`
  - 버튼명: `초기화`
  - 위치: `O2`
  - 역할 추정: 보고서 상단 불러온 계약 영역 초기화

- `FormatSelectedRange`
  - 버튼명: `폰트/정렬`
  - 위치: `O5`
  - 역할 추정: 선택 영역 서식 정리

### `신규계약리스트`

- `CopyFilteredData`
  - 버튼명: `Update`
  - 역할 추정: 필터된 계약 리스트를 복사하거나 `백데이터`/보고서용 구조로 가공

추가로 바이너리 문자열 기준 아래 프로시저명도 확인됨

- `SummarizeAndMergeAtBottom`

주의

- `vbaProject.bin` 존재는 확인했고 `Module1` ~ `Module5` 모듈 흔적도 확인했다.
- 다만 현재 세션에서는 VBA 본문 소스까지 직접 열람되지는 않았다.
- 따라서 위 내용은 버튼 연결명과 시트 데이터 흐름을 기반으로 한 기능 추정이다.

## 4. 시트별 역할

## `주간실적보고`

보고서 최종 출력 시트.

주요 영역

- 상단 제목: `A2:N4`
- 계약 리스트: `A5:M22`
- 매출 요약: `A24:N30`
- 단말기 순증/해지 요약: `A32:L39`
- 연간 목표/실적: `A41:M55`
- 유료 옵션 정보: `A57:N69`
- 해지 현황: `A71:L75`
- 업종별 실적: `A77:L80`

특징

- 상단 계약 리스트는 상수값으로 채워져 있으며 버튼을 통한 불러오기 흐름이 존재
- KPI 문구와 표 하단 합계는 일부 수식
- `Print_Area = A2:N80`
- PDF 출력용 메인 시트로 판단

## `옵션정보입력`

명시적으로 `수동 업데이트 영역`이라고 적힌 운영 입력 시트.

주요 입력 블록

- `A3:N9`: 월별 매출순증/위약금/이전비/합계
- `Q4:V8`: 추가 매출 리스트
- `A11:L18`: 단말기 순증/해지, 경쟁사 교체, 해지대기/청구보류, 해지유형
- `A20:M34`: 연간 목표/실적
- `A36:N48`: 유료 옵션 정보
- `A50:L54`: 해지 현황
- `A56:L59`: 업종별 실적

특징

- 자동 참조보다 사람이 직접 숫자와 문구를 갱신하는 구조가 강함
- 웹 대시보드 전환 시 가장 먼저 폼/입력 화면으로 치환해야 하는 시트

## `신규계약리스트`

신규 계약 마스터 시트.

주요 구조

- `A3:R111`: 계약 원장
- `T3:AC12`: 월별/권유자별 통계

주요 컬럼

- `A`: 실적반영
- `B`: No.
- `C`: 회사명
- `E`: 부서
- `G`: ID
- `I`: 업종
- `K`: 계약월
- `M/O`: 계약서 회수 여부
- `Q`: 권유자
- `R`: 대체여부
- `S`: 비고

통계 수식

- `U:AB`: 권유자별 건수
- `AC`: 합계
- 수식 패턴: `COUNTIFS($K:$K,$T행,$Q:$Q,담당자헤더)`

특징

- 실제 신규 계약 데이터의 단일 원천(source of truth) 역할
- 이후 파생 엑셀들이 있다면 이 시트 또는 이와 같은 원장 데이터가 공통 기준이 되어야 함

## `백데이터`

숨김 시트이며 현재는 16건만 저장되어 있음.

구성

- `A:O` 사이에 회사명, 부서, ID, 업종, 계약월, 회수여부, 권유자 등이 축약 저장
- `주간실적보고` 상단 계약 리스트와 같은 성격의 중간 데이터

추정 역할

- `신규계약리스트`에서 조건에 맞는 데이터만 추려 보고서 전면에 넣기 위한 버퍼

## 5. 확인된 수식 구조

### `주간실적보고`

주요 수식 유형

- 날짜 기준 문구 생성
  - `A4 = TODAY()+1`
  - 보고 기준일을 다음날 기준으로 표기

- 월별 매출 합계
  - `N26:N29`, `B29:M29`
  - `SUM` 중심

- 매출 설명 문구
  - `F24`
  - 순증 매출, 연간 누적 매출을 문장으로 조합

- 연간 목표/실적 누계
  - `E44:E54`, `C55:M55`

- 옵션/해지/업종별 합계 및 비율
  - `B58:N58`
  - `L73:L75`
  - `L79:L80`

### `옵션정보입력`

거의 동일한 계산 구조를 더 작은 레이아웃으로 반복

- 월별 매출 합계: `N5:N8`, `B8:M8`
- 설명 문구: `F3`
- 목표 누계: `E23:E33`, `C34:M34`
- 옵션/해지/업종별 합계: `B37:N37`, `L52:L54`, `L58:L59`

### `신규계약리스트`

통계 전용 수식

- `U4:AB11`: `COUNTIFS`
- `AC4:AC11`: 월별 합계
- `U12:AC12`: 담당자 열 합계

## 6. 수동 입력이 필요한 것으로 보이는 영역

실무상 수동 입력 또는 수동 검증이 필요한 포인트는 아래로 보인다.

### 강한 수동 입력 영역

- `옵션정보입력` 거의 전체
- `신규계약리스트` 원장 행 데이터
- `주간실적보고` 상단 계약 리스트 외의 각종 요약 문구/숫자

### 수동 + 매크로 혼합 영역

- `주간실적보고` 상단 계약 리스트
- `백데이터`

### 수동 교차검증 포인트

- `신규계약리스트` 원장 건수
- `주간실적보고` 신규/순증/해지/업종별 수치
- `옵션정보입력`의 각 표 합계
- `추가 매출 리스트`와 매출 요약 문구

## 7. 웹 대시보드 전환 시 핵심 설계 포인트

엑셀을 그대로 웹으로 옮기기보다 아래처럼 역할을 분해하는 것이 맞다.

1. 계약 원장 관리
   - `신규계약리스트`를 DB 테이블로 전환
   - 회사명, 부서, ID, 업종, 계약월, 계약서 회수여부, 권유자, 대체여부, 비고를 표준 컬럼화

2. 수동 KPI 입력 관리
   - `옵션정보입력`을 관리자 입력 폼으로 전환
   - 주차/기준일 단위로 저장

3. 보고서 생성
   - `주간실적보고`를 읽기 전용 대시보드 + PDF 출력 템플릿으로 전환
   - 계약 리스트는 필터 조건으로 자동 반영
   - KPI 문구는 계산식으로 자동 생성

4. 파생 파일 제거
   - 중복 엑셀 대신 단일 데이터 저장소에서 각 보고서/통계 화면을 재생성
   - 크로스체크는 수작업이 아니라 검증 규칙으로 대체

## 8. 바로 다음으로 권장하는 작업

웹 전환 전에 아래를 추가 확인하면 설계가 훨씬 정확해진다.

1. VBA 본문 추출
   - 각 버튼이 실제로 어떤 범위를 복사/초기화/포맷하는지 확인 필요

2. 파생 엑셀 목록 확보
   - 어떤 파일들이 이 원본에서 파생되는지, 컬럼 중복이 무엇인지 확인 필요

3. 주간 보고서 작성 순서 인터뷰
   - 누가 어느 시트부터 업데이트하는지
   - 어떤 숫자를 가장 많이 크로스체크하는지
   - PDF는 어떤 레이아웃으로 꼭 유지해야 하는지

4. 기준 키 확정
   - `ID`, `회사명`, `계약월`, `권유자` 중 무엇이 실적 집계의 기준 키인지 정의 필요
