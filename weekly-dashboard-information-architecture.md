# 정보사업본부 주간실적 웹 대시보드 설계안

## 1. 목표

현재 엑셀 기반 주간실적보고 업무를 아래 형태의 웹 시스템으로 전환한다.

- 웹에서 신규 계약 원장 업데이트 가능
- 웹에서 주간 KPI와 수동 입력 항목 업데이트 가능
- 자동 집계와 교차검증 가능
- 동일 데이터로 대시보드 조회와 PDF 출력 가능
- 파생 엑셀 없이 단일 원천 데이터로 운영 가능

## 2. 설계 원칙

이번 구조는 엑셀 시트 이름을 그대로 웹 메뉴로 옮기지 않고, 실제 업무 역할 기준으로 나눈다.

핵심 원칙

- 원장과 보고서를 분리한다
- 수동 입력과 자동 집계를 구분한다
- PDF는 결과물이고, 입력은 모두 웹에서 처리한다
- 중복 입력을 없애고 검증 규칙으로 크로스체크를 대체한다
- 주간 보고서 한 건은 `기준일` 또는 `주차` 단위의 스냅샷으로 저장한다

## 3. 사용자 기준 운영 흐름

매주 업무 흐름은 아래 순서가 가장 자연스럽다.

1. 영업담당자가 `신규 계약 원장`에 자기 계약 리스트 업데이트
2. 수요일에 운영 담당자 1명이 이번 주 반영 대상을 선택
3. 시스템이 선택된 반영 대상만 `주간 보고서 계약 리스트`에 불러옴
4. 운영 담당자가 `주간 보고 입력`에서 옵션/KPI 수동 값 입력
5. 검증 화면에서 불일치 확인
6. 보고서 화면에서 최종 확인
7. PDF 생성 및 보고

## 3-1. 현재 엑셀 프로세스를 웹 메뉴로 옮긴 매핑

엑셀의 실제 순서는 아래처럼 웹 기능으로 치환된다.

1. `신규계약리스트`에 영업담당자들이 각자 입력
   - 웹에서는 `신규 계약 원장`

2. 운영 담당자 1명이 `실적반영(A열)` 선택 후 `Update`
   - 웹에서는 `주간 반영 확정` 액션

3. `주간실적보고`에서 `초기화` 후 `계약호출`
   - 웹에서는 `반영 대상 계약 자동 불러오기`

4. `옵션정보입력` 수동 업데이트
   - 웹에서는 `주간 보고 입력`

5. `주간실적보고` PDF 출력
   - 웹에서는 `보고서 / PDF`

## 4. 권장 메뉴 구조

좌측 메뉴 기준으로 아래 6개 섹션을 권장한다.

1. `대시보드`
2. `신규 계약 원장`
3. `주간 반영 관리`
4. `주간 보고 입력`
5. `검증 센터`
6. `보고서/PDF`
7. `기준정보 관리`

## 5. 화면 설계

## 5-1. 대시보드

가장 먼저 열리는 메인 화면.

목적

- 이번 주 실적을 한눈에 요약
- 전주 대비 증감 확인
- 아직 입력되지 않은 항목 또는 검증 오류 표시

표시 카드

- 주간 순증 매출
- 연간 누적 매출
- 주간 신규 계약 대수
- 주간 해지 대수
- 누적 순증
- 총 계약 대수
- 유료 옵션 총 건수
- 업종별 신규/순증 요약

추가 요소

- `이번 주 보고서 작성 상태`
- `검증 필요 항목`
- `최근 PDF 생성 이력`

상단 필터

- 기준일
- 연도
- 주차
- 담당자
- 업종

## 5-2. 신규 계약 원장

현재 `신규계약리스트` 시트를 대체하는 핵심 화면.

목적

- 신규 계약 데이터를 직접 등록, 수정, 검색
- 보고서에 쓰이는 모든 계약 데이터의 단일 원천 역할

메인 테이블 컬럼

- 실적반영 여부
- No. 또는 내부 일련번호
- 회사명
- 부서
- ID
- 업종
- 계약월
- 계약서 회수 여부
- 권유자
- 대체여부
- 비고
- 등록일
- 수정일

주요 기능

- 신규 등록
- 행 수정
- 엑셀 업로드
- 다건 수정
- 계약월/권유자/업종 필터
- `보고서 반영 대상만 보기`
- 회수/미회수 필터

우측 요약 패널

- 월별 신규 건수
- 권유자별 건수
- 업종별 건수
- 대체유형별 건수

중요 포인트

- 엑셀의 우측 `COUNTIFS` 통계는 별도 테이블 계산이 아니라 이 화면의 집계 패널과 API로 대체
- 수동 정렬이 아니라 필터 저장 기능을 제공하는 편이 좋다
- 영업담당자는 여기까지만 주로 사용하고, 주간 반영 확정은 별도 담당자가 수행

## 5-3. 주간 반영 관리

이 화면은 현재 엑셀의 `실적반영(A열) 선택 + Update + 계약호출` 흐름을 웹으로 옮긴 것이다.

목적

- 이번 주 보고서에 포함할 계약을 운영 담당자 1명이 확정
- 확정된 계약만 보고서 상단 리스트로 자동 반영

핵심 기능

- 기준 주차 선택
- 계약 원장에서 반영 대상 조회
- 계약별 `실적반영` 체크
- `이번 주 반영 확정` 버튼
- `보고서 계약 리스트 미리보기`

중요 포인트

- 현재 엑셀에서는 `A열 실적반영`이 매우 중요한 승인 개념이므로 웹에서도 반드시 유지해야 함
- 이 단계가 있어야 영업 원장 전체와 실제 보고 반영 대상을 분리할 수 있음
- 엑셀의 `초기화`는 웹에서는 `현재 주차 반영 목록 재생성` 개념으로 치환 가능

## 5-4. 주간 보고 입력

현재 `옵션정보입력` 시트를 대체하는 화면.

주간 보고서는 한 번에 모든 항목을 입력해야 하므로, 한 화면 안에서 탭 또는 섹션으로 나누는 것이 좋다.

권장 탭 구성

1. `매출`
2. `계약/해지`
3. `연간 목표`
4. `유료 옵션`
5. `해지 현황`
6. `업종별 실적`
7. `추가 매출`

### 탭 1. 매출

입력 항목

- 월별 매출순증
- 월별 위약금
- 월별 이전비

자동 계산

- 월별 합계
- 연간 순증 매출 문구
- 연간 누적 매출 문구

### 탭 2. 계약/해지

입력 항목

- 주간 신규계약
- 주간 계약해지
- 누적 신규계약
- 누적 계약해지
- 총 계약 대수
- 경쟁사 교체 현황
- 해지대기
- 청구보류
- 해지 진행 현황
- 단말 해지 유형 요약

자동 계산

- 주간 순증 합계
- 누적 순증 합계

### 탭 3. 연간 목표

입력 항목

- 월별 순증 목표
- 월별 목표 계약대수
- 분기 순증 목표
- 월간 실적

자동 계산

- 누적 목표 계약대수
- 분기 실적
- 목표 대비 달성 현황
- 연간 합계

### 탭 4. 유료 옵션

입력 항목

- 옵션 종류별 건수
- 업종별 건수

표현 방식 권장

- 엑셀형 가로표보다 `옵션별 카드 + 업종 상세 테이블` 구조가 더 적합

### 탭 5. 해지 현황

입력 항목

- 계약만료
- 비용절감
- 퇴사
- 조직개편
- 휴직/장기출장
- 합병매각
- 활용저조
- 타사대체
- 비용미납

자동 계산

- 주간 합계
- 누적 합계
- 사유별 비율

### 탭 6. 업종별 실적

입력 항목 또는 자동 집계 선택 가능

- 국내증권
- 국내은행
- 외국계
- 자산운용
- 보험사
- 일반기업
- 공사/정부
- 연기금
- 기타금융

권장 방식

- 신규 건수는 원장에서 자동 집계
- 순증은 주간 입력값과 연동 또는 별도 수동 보정 입력 허용

### 탭 7. 추가 매출

현재 `Q:V` 영역을 별도 그리드로 분리.

컬럼

- No.
- ID
- 회사
- 금액
- 내용
- 비고

주요 기능

- 다건 입력
- 금액 자동 합산
- 특정 주차에만 포함

중요 포인트

- 이 화면은 자동 계산 화면이 아니라 `최종 수동 확정값 입력 화면`이어야 함
- 특히 옵션, 해지 현황, 목표 실적 등은 현재 업무상 수동 입력을 전제로 설계하는 편이 안전함

## 5-5. 검증 센터

이 화면이 엑셀 크로스체크 수작업을 없애는 핵심이다.

목적

- 입력 누락, 수치 불일치, 기준 위반 자동 탐지

대표 검증 규칙

- 원장 신규 건수와 보고서 신규 건수가 일치하는지
- 해지 사유 합계와 해지 총계가 일치하는지
- 업종별 합계와 전체 합계가 일치하는지
- 옵션별 업종 합계와 옵션 총계가 일치하는지
- 계약서 회수 여부 미입력 건이 있는지
- 권유자 미지정 건이 있는지
- PDF 생성 전 필수 항목이 모두 입력됐는지

표현 방식

- `오류`
- `주의`
- `확인 필요`

각 항목은 클릭 시 바로 수정 화면으로 이동하도록 설계한다.

## 5-6. 보고서/PDF

현재 `주간실적보고` 시트를 대체하는 최종 출력 화면.

목적

- 실제 임원/부서 보고용 레이아웃 제공
- 웹 화면과 PDF 결과물이 최대한 동일하게 보이도록 함

구성

- 상단 보고 제목
- 기준일
- 계약 리스트
- 주간 순증 매출
- 단말기 순증 및 해지
- 경쟁사 교체 현황
- 해지대기 및 청구보류
- 해지 유형
- 연간 목표/실적
- 유료 옵션 현황
- 해지 현황
- 업종별 실적 현황

기능

- PDF 생성
- 인쇄
- 보고서 잠금
- 과거 주차 버전 조회

중요 포인트

- PDF 레이아웃은 현재 엑셀 보고서 느낌을 유지하되, 웹에서는 더 읽기 쉽게 카드+테이블 혼합 구조를 쓴다
- 단, PDF는 현행 보고 양식과 최대한 비슷하게 유지하는 것이 조직 적응에 유리하다

## 5-7. 기준정보 관리

운영자가 관리하는 설정 화면.

관리 항목

- 권유자 목록
- 업종 코드
- 대체유형 코드
- 옵션 종류
- 보고서 기본 문구
- 연간 목표값
- PDF 서명/부서명

이 화면이 있어야 매년 엑셀 수식/문구를 뜯어고치지 않아도 된다.

## 6. 데이터 흐름 설계

화면 간 흐름은 아래처럼 가져가는 것이 좋다.

### 흐름 A. 계약 데이터

`신규 계약 원장` 입력
-> `주간 반영 관리`에서 반영 대상 선택
-> 선택된 계약만 `보고서/PDF` 상단 계약 리스트로 반영

### 흐름 B. 주간 KPI 데이터

`주간 보고 입력` 저장
-> 검증 규칙 실행
-> 통과 시 `보고서/PDF` 반영

### 흐름 C. 최종 보고

`보고서/PDF` 화면 검토
-> PDF 생성
-> 생성 시점 데이터를 주차 스냅샷으로 보관

## 7. 화면별 권한 제안

권한은 최소 3단계가 적당하다.

### 운영자

- 원장 수정 가능
- 주간 반영 확정 가능
- 기준정보 수정 가능
- 보고서 확정 가능
- PDF 생성 가능

### 실무자

- 원장 등록/수정 가능
- 주간 보고 입력 가능
- 검증 오류 확인 가능
- PDF 조회 가능

### 조회자

- 대시보드 조회
- 확정된 보고서/PDF 조회

## 8. 웹 화면에서 특히 좋아지는 부분

엑셀 대비 개선점은 명확하다.

- 신규 계약 원장과 보고서 입력이 분리되어 중복 수정이 줄어듦
- 집계가 자동화되어 `COUNTIFS`나 합계 수식 검증 부담이 사라짐
- 누락/불일치를 검증 센터에서 바로 발견 가능
- 같은 데이터를 여러 엑셀로 복사하지 않아도 됨
- 특정 주차 PDF를 언제든 재생성 가능

## 9. 첫 화면 와이어프레임 개념

### 메인 대시보드

- 상단: 기준일 선택, 주차 선택, PDF 바로가기
- 1행: KPI 카드 6개
- 2행 왼쪽: 신규 계약 추이
- 2행 오른쪽: 해지 사유 분포
- 3행 왼쪽: 권유자별 실적
- 3행 오른쪽: 업종별 실적
- 하단: 검증 경고 목록, 최근 보고서 이력

### 보고서/PDF 화면

- 상단: 제목, 기준일, 작성 상태, PDF 버튼
- 본문 1: 계약 리스트 테이블
- 본문 2: 매출/순증/해지 요약 카드
- 본문 3: 연간 목표/실적 표
- 본문 4: 옵션/해지/업종별 현황 표

## 10. 추천 개발 순서

웹으로 옮길 때는 한 번에 전체를 만들기보다 아래 순서가 가장 안전하다.

1. `신규 계약 원장`
2. `주간 반영 관리`
3. `주간 보고 입력`
4. `검증 센터`
5. `보고서/PDF`
6. `기준정보 관리`

이 순서로 가야 파생 엑셀 제거와 데이터 일원화가 먼저 달성된다.

## 11. 이번 파일 기준으로 화면과 엑셀의 매핑

- `신규계약리스트` -> `신규 계약 원장`
- `실적반영(A열) + Update` -> `주간 반영 관리`
- `옵션정보입력` -> `주간 보고 입력`
- `주간실적보고` -> `보고서/PDF`
- `백데이터` -> 내부 집계/스냅샷 테이블로 대체

## 12. 다음 단계 제안

이 설계 다음으로는 아래 순서가 자연스럽다.

1. 메뉴별 상세 입력 항목 정의서 작성
2. DB 테이블 설계
3. 화면 와이어프레임 초안 제작
4. MVP 범위 확정
